# Tandoor Recipes connection (required unless configured via the setup wizard)
TANDOOR_URL=http://localhost:8080
TANDOOR_TOKEN=your-api-token-here

# Logging
LOG_LEVEL=INFO
# LOG_TO_STDOUT=1          # Set to 1 in Docker (enabled by default in Dockerfile)
